name: start easyLang
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ../
  
    steps:
      - name: execute py script # run the example.py to get the latest data
        run: |
          python example.py
